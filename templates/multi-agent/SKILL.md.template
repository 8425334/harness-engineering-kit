---
name: multi-agent
description: 前后端联动双Agent并行模式：自动检测任务边界，定义共享契约，并行启动Agent-BE(RAMER)+Agent-FE(RADIR)，合并验证字段对齐。使用 /multi-agent <需求描述> 触发。
args: <requirement description covering both frontend and backend>
---

# 多 Agent 并行模式 — 前后端联动

当任务**同时涉及前端和后端**代码变更时，启用双 Agent 并行执行，最大化效率。

## 触发方式

```
/multi-agent <需求描述>         # 完整双Agent并行工作流
```

## 适用场景

- 新增业务功能（需要后端接口 + 前端页面）
- 修改现有功能（前后端都需要改）
- API 契约变更（字段增删改）
- 跨模块重构

## 执行流程

```
用户需求（前后端联动）
       │
       ▼
┌─ 阶段 0: 契约定义（主 Agent）─────────────────────┐
│                                                    │
│  1. 识别前后端边界                                  │
│  2. 提取共享数据契约：                              │
│     - API 端点 + HTTP 方法                          │
│     - 请求参数字段 + 类型                           │
│     - 返文字段 + 类型                               │
│     - 枚举/状态码                                   │
│  3. 输出为双方 Agent 的共享上下文                    │
│  4. 向用户确认契约，确认后进入并行阶段               │
│                                                    │
└────────────────────────────────────────────────────┘
       │
       ├──────────────────┐
       ▼                  ▼
┌─ Agent-BE ────────┐  ┌─ Agent-FE ───────────────┐
│ (background)      │  │ (background)             │
│                   │  │                          │
│ RAMER 循环:       │  │ RADIR 工作流:            │
│ R→A→M→E→R        │  │ R→A→D→I→V               │
│                   │  │                          │
│ 基于契约实现:     │  │ 基于契约实现:            │
│ DTO/BO/VO        │  │ types/API 封装           │
│ Controller        │  │ views/components         │
│ Service/Mapper   │  │ 路由/i18n                │
│ fitness 门禁     │  │ typecheck+build 门禁     │
└──────────────────┘  └──────────────────────────┘
       │                  │
       └────────┬─────────┘
                ▼
┌─ 阶段 2: 合并验证（主 Agent）─────────────────────┐
│                                                    │
│  1. 字段对齐：后端返文字段 vs 前端类型定义           │
│  2. 枚举一致：后端枚举值 vs 前端常量                 │
│  3. 路由同步：新增页面是否注册                       │
│  4. 权限同步：按钮编码是否一致                       │
│  5. 如有不一致 → 修复 → 重新验证                     │
│                                                    │
└────────────────────────────────────────────────────┘
```

## 阶段 0: 契约定义（关键步骤）

这是并行执行的基础。契约必须包含：

### API 契约模板

```yaml
endpoints:
  - method: GET/POST/PUT/DELETE
    path: /api/xxx/xxx
    summary: 接口用途
    request:
      params:  # Query 参数
        - name: pageNum, type: int, required: true, desc: 页码
      body:  # Request Body 字段
        - name: fieldName, type: string, required: true, desc: 字段说明
    response:
      - name: id, type: long, desc: 主键
      - name: fieldName, type: string, desc: 字段说明
    enums:
      - name: StatusEnum, values: [ACTIVE, INACTIVE, DELETED]
```

### 检测前后端边界

根据项目 CLAUDE.md 和目录结构自动检测（替换为项目实际目录）：
- **后端目标目录**：Java 模块（如 `coil-service/`、`coil-app/`、`ruoyi-*`）
- **前端目标目录**：前端工程源码（如 `coil-backend-ui/src/` 或框架对应的 `src/`）
- **契约共享方式**：API 类型定义文件、枚举常量文件

### 用户确认

契约定义完成后，向用户确认：
- API 端点列表、请求/返文字段、枚举值
- 确认后并行启动两个 Agent

## 阶段 1: 并行启动

确认契约后，同时启动两个后台 Agent：

### Agent-BE（后端）

```
Agent type: general-purpose
Prompt 内容:
- RAMER 循环执行说明
- 项目架构上下文（从 CLAUDE.md 读取）
- 契约字段详情
- 目标目录列表
- fitness gate 命令

职责:
1. 按 RAMER 循环实现后端代码
2. Controller / Service / Mapper / DTO / VO / BO
3. SQL 迁移（如需要）
4. 权限注解
5. 运行 fitness gate
```

### Agent-FE（前端）

```
Agent type: general-purpose
Prompt 内容:
- fe-engineering RADIR 工作流说明（4铁律 + 组件分解）
- 技术栈上下文（从 package.json 检测）
- 契约字段详情
- 目标目录列表
- 质量门禁命令

职责:
1. 按 RADIR 工作流实现前端代码
2. types / API 层 / views / components
3. 路由注册 / i18n
4. 运行 typecheck + build
```

### 并行启动规则

1. 两个 Agent 通过 `run_in_background: true` 同时启动
2. 各自独立执行，不互相等待
3. 主 Agent 不轮询，等待系统通知完成
4. 双方完成后，主 Agent 进入合并验证阶段

## 阶段 2: 合并验证

双方 Agent 完成后，执行交叉验证：

### 字段对齐检查
- 后端 Controller 返文字段 vs 前端 types 定义
- 请求参数字段 vs 前端 API 调用参数
- 检查是否有字段遗漏或命名不一致

### 枚举一致性
- 后端枚举类值 vs 前端常量/枚举
- 状态码映射是否完整

### 路由同步
- 新增页面是否已在路由配置中注册
- 菜单配置是否更新

### 权限同步
- 后端权限注解 vs 前端按钮权限编码
- 确保权限编码一致

### 问题修复
- 发现不一致 → 直接修复 → 重新验证
- 无法自动修复 → 列出差异，请用户手动处理

## 降级策略

- 子 Agent 不可用（如后台 Agent 超限）→ 序列执行：先后端契约实现，再前端表现层
- 单侧任务（仅前端或仅后端）→ 不启用并行，直接用对应工作流（/fe 或 /ramer）
- 契约确认被拒绝 → 重新讨论需求，调整契约后再次确认

## Agent 提示词模板

### Agent-BE 提示词核心要素

```
You are implementing the BACKEND part of a full-stack feature.

## Contract (shared with frontend)
[完整的 API 契约 YAML]

## Your Task
Implement all backend changes following the RAMER cycle:
READ → ANALYZE → MODEL → EXECUTE → REVIEW

## Architecture Context
[从 CLAUDE.md 读取的模块依赖图、编码规范]

## Deliverables
- Controller endpoints with permission annotations
- Service layer (interface + impl)
- DTO/VO/BO classes
- Mapper + XML (if needed)
- SQL migration (if schema changes)
- Pass fitness gate
```

### Agent-FE 提示词核心要素

```
You are implementing the FRONTEND part of a full-stack feature.

## Contract (shared with backend)
[完整的 API 契约 YAML]

## Your Task
Implement all frontend changes following fe-engineering RADIR workflow:
READ → ANALYZE → DECOMPOSE → IMPLEMENT → VERIFY

## Tech Stack
[从 package.json 检测的框架和 UI 库]

## Iron Rules
1. Layer discipline: views -> API layer -> request layer
2. Types as contract: no `any`, full TypeScript types
3. Controlled components: files <= 300 lines, modal/drawer/form in modules/
4. Three-state coverage: Loading / Empty / Error for every data fetch

## Deliverables
- API types in typings/api/
- API methods in service/api/
- Views and components (following decomposition rules)
- Route registration
- i18n entries
- Pass typecheck + build
```
