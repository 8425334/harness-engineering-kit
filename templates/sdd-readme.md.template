# SDD 过程文档（Spec-Driven Development）

<!--
  Copy to: docs/sdd/README.md
  Fill in {{PLACEHOLDER}} values.
-->

## 概述

SDD（Spec-Driven Development）是本项目非 trivial 变更的规范流程。每个有意义的变更（新功能、跨模块修改、新增领域对象）必须经过 Spec → Design → Tasks 的文档化流程，再进入实现。

## 流程

```
Explore (可选) → Propose → Apply → Sync → Archive
```

| 步骤 | 命令 | 说明 | 工作流映射 |
|------|------|------|-----------|
| Explore | `{{EXPLORE_COMMAND}}` | 思考问题、澄清需求（可选） | Pre-开发 |
| Propose | `{{PROPOSE_COMMAND}}` | 创建 change，生成 proposal + specs + design + tasks | R → A → M |
| Apply | `{{APPLY_COMMAND}}` | 按任务清单逐步实现 | E |
| Sync | `{{SYNC_COMMAND}}` | 将 delta specs 合入主规格库 | R (review) |
| Archive | `{{ARCHIVE_COMMAND}}` | 归档完成 change | R (review) |

每个步骤内部遵循开发铁律：先加载路径文档（R），再分析边界（A），再建模设计（M），再实现（E），再 fitness 验证（R）。

## Change 生命周期

```bash
# 创建新 change
{{CREATE_CHANGE_COMMAND}}

# 查看状态
{{STATUS_COMMAND}}
```

change 目录位于 `{{CHANGE_DIR}}`，包含 4 类产物：

| 产物 | 文件 | 生成顺序 |
|------|------|---------|
| Proposal | `proposal.md` | 第 1 |
| Specs (delta) | `specs/<capability>/spec.md` | 第 2 |
| Design | `design.md` | 第 3 |
| Tasks | `tasks.md` | 第 4 |

产物按依赖顺序生成：proposal 定义 scope → specs 定义需求 → design 决策实现方案 → tasks 拆分实施步骤。

完成后归档至 `{{ARCHIVE_DIR}}`。

## 与 Path-Document 集成

创建任何 SDD change 之前，先加载受影响区域的路径文档：

```bash
{{PATH_DOC_LOADER}}
```

路径文档提供架构上下文（模块职责、依赖方向、禁止内容），proposal 和 design 应遵守这些约束。

## 与 Fitness 集成

实现完成后，必须通过 fitness gate：

```bash
{{FITNESS_COMMAND}}
```

## 本项目特化约定

### 任务分组顺序

Tasks 按以下模块分组顺序组织：

```
1. Domain Contract — Entity, VO, BO, DTO
2. Support — 复杂编排逻辑
3. Service — 业务入口
4. Controller — API 端点
5. Data Access — Mapper / Repository
6. Schema Migration — 表结构/权限变更
7. Verification — 编译 + fitness gate + 一致性检查
8. Frontend — UI 组件 + API 文件
```

### Spec 约定

- 需求用 `SHALL` / `MUST`（不用 should/may）
- 场景用 `#### Scenario: <name>` + `WHEN/THEN` 格式
- 引用具体模块名和路径

### Design 约定

- 决策用 D1/D2...编号，含 choice / reason / alternatives
- 每个决策必须指定实现模块归属

### 变更适用范围

| 变更类型 | 是否走 SDD | 说明 |
|----------|-----------|------|
| 新功能 / 新领域对象 | **是** | 全链路 |
| 跨模块修改 | **是** | 需要协调多个模块 |
| Bug 修复 / 小调整 | **否** | 直接 RAMER + fitness 验证 |
| 重构 | **视规模** | 大规模重构走 SDD，局部重构不需要 |
