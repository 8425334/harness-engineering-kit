# 本轮实施契约（Codex）

> 每轮实施开始前更新。Codex 当前没有 Claude `PreCompact`/`SessionStart` 的项目级 hook，
> 因此在长轮次或预计发生上下文压缩前运行 `.codex/hooks/save-state.sh`，恢复后重新读取
> 本文件及 `.codex-state/` 下最新存档。保持精炼（≤50 行），只记录必须跨压缩保留的事实。

## 当前任务（本轮实施）

- 任务名：<!-- 例如 WP4 adapter 实现 -->
- 目标一句话：<!-- 本轮要交付什么 -->
- 所在模块：<!-- 例如 customer-application / order-service -->

## 关键契约字段

- API 端点 / 方法：<!-- 例如 POST /api/v1/<domain>/<action> -->
- 请求入参（BO）：<!-- 字段名:类型 -->
- 返回出参（VO/DTO）：<!-- 字段名:类型 -->
- 枚举 / 状态码：<!-- 与前端常量一致的枚举值 -->

## 相关文件清单

- <!-- 后端：Adapter/Application/Domain/Infra/PO/Mapper -->
- <!-- 前端：API 层 / 页面 / 组件 / 路由 / i18n -->

## 当前待办（本轮）

- [ ] <!-- 第一步 -->
- [ ] <!-- 第二步 -->

## 约束 / 坑（压缩后不能忘）

- <!-- 例如：DTO 转换只在 Controller；manifest 要同步 -->
