# 本轮实施契约（round contract）

> 用途：在**每轮实施开始前**更新本文件；当会话发生 auto-compact 后，
> SessionStart(matcher=compact) hook 会把本文件重新注入上下文，保证压缩后仍保有
> 本轮目标、契约字段、文件清单与待办，不丢失实施线索。
> 规则：本文件保持精炼（≤50 行），只放"压缩后必须还记得"的事实。

## 当前任务（本轮实施）

- 任务名：<!-- 例如 WP4 adapter 实现 -->
- 目标一句话：<!-- 本轮要交付什么 -->
- 所在模块：<!-- 例如 customer-application / order-service -->

## 关键契约字段

- API 端点 / 方法：<!-- 例如 POST /api/v1/<domain>/<action> -->
- 请求入参（BO）：<!-- 字段名:类型 -->
- 返回出参（VO/DTO）：<!-- 字段名:类型 -->
- 枚举 / 状态码：<!-- 与前端常量一致的枚举值 -->

## 相关文件清单

- <!-- 后端：Adapter/Application/Domain/Infra/PO/Mapper -->
- <!-- 前端：API 层 / 页面 / 组件 / 路由 / i18n -->

## 当前待办（本轮）

- [ ] <!-- 第一步 -->
- [ ] <!-- 第二步 -->
