---
dimension: architecture_boundary
weight: 10
tier: fast
metrics:
  - name: architecture_boundary_baseline
    command: python3 docs/fitness/scripts/check_architecture_boundary.py
    pattern: "Architecture boundary baseline ok"
    hard_gate: true
    tier: fast
---

# 架构边界规则

## 当前目标

- 将文档中的模块边界转为第一批可执行静态检查。
- 第一阶段只检查低误报的路径与目录规则。

## 当前检查

- 基础设施/共享模块（`{{INFRA_MODULE}}`）下不应出现业务包（路径含 `{{INFRA_FORBIDDEN_PART}}`）。
- 应用/领域模块（`{{APP_MODULE}}`）下不应出现编排入口（`{{APP_FORBIDDEN_GLOB}}`）。
- 新增非 trivial 目录 `docs/fitness` 必须包含 `AI.md` 和 `ai.json`。

## 后续扩展

- 依赖方向检查（Maven / Gradle / npm 依赖图）。
- Mapper 接口与 XML 匹配检查。
- 重复 DAL 和跨模块业务编排检查。
