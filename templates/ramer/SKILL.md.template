---
name: ramer
description: Fully automated RAMER cycle with DDD architecture — CRUD / DDD / Hexagonal / Hybrid. Use /ramer <requirement> to trigger.
args: <requirement description for the code change>
---

# RAMER — 全自动设计实现循环（DDD 增强版）

一键触发 RAMER 全流程。**自动检测架构风格**（CRUD/DDD/Hexagonal/Hybrid），产出相应设计契约，人工确认后自动实现 + 审查。

## 架构自适应

系统根据需求复杂度自动判定架构风格：

| 信号 | 风格 | 契约产出 |
|------|------|---------|
| 简单 CRUD，无不变量 | **crud** | Entity Quartet (Entity/VO/BO/Mapper/XML) 或等效模式 |
| 复杂业务规则、状态机、不变量 | **ddd** | Aggregate + ValueObject + DomainService + Port + Adapter + ApplicationService |
| 多外部系统、已有 port/adapter | **hexagonal** | Port + Adapter + ApplicationService |
| 混合场景 | **hybrid** | 组合 CRUD + DDD |

**关键规则**：若目标区域已有 hexagonal-ddd（port/, adapter/, domain/model/），必须延续该模式。

## 项目上下文自动检测

在开始 RAMER 循环前，先读取项目的 CLAUDE.md 以检测以下项目特定配置：

### 路径文档加载
检查 CLAUDE.md 中是否配置了路径文档加载脚本。常见模式：`python3 ~/.codex/skills/*/scripts/find_ai_context.py <target-path>`。如已配置，严格按返回的文档列表读取。如未配置，手动读取目标路径及父目录的 `AI.md` / `ai.json` 文件。

### 持久层框架检测
检查项目依赖（pom.xml / build.gradle）和 CLAUDE.md，确定：
- **ORM 框架**：MyBatis-Plus / JPA / Hibernate / Jdbi / 其他
- **实体基类**：如 `TenantEntity`、`BaseEntity` 或标准 JPA Entity
- **主键策略**：如 `@TableId(type=ASSIGN_ID)`（雪花）、`@Id @GeneratedValue`（自增）
- **逻辑删除**：如 `@TableLogic`、`@SQLDelete`
- **Mapper/Repository 基类**：如 `BaseMapperPlus<Entity, Vo>`、`JpaRepository<Entity, Long>`
- **Entity↔VO 映射**：如 `@AutoMapper(target=Entity.class)`、MapStruct、手写转换

### 安全框架检测
检查项目依赖确定权限框架（Sa-Token / Spring Security / Shiro / 自定义），CLAUDE.md 中通常声明了权限注解约定。应用到 Controller 层的权限注解格式。

### Fitness Gate 检测
检查 CLAUDE.md 中是否配置了 fitness gate 命令。常见模式：`python3 docs/fitness/scripts/fitness.py --tier fast`。如已配置，在 REVIEW 阶段运行。如未配置，执行手动检查清单。

### 模块与包路径
从项目结构和 CLAUDE.md 模块依赖图中检测：
- **业务基础包**：如 `org.dromara.coil`
- **基础设施模块**：如 `ruoyi-common`、`coil-app/coil-common`
- **SQL 迁移路径**：如 `script/sql/update/`

### DDD 参考实现
检查项目中是否存在已有的 hexagonal-ddd 模块（含 port/、adapter/、domain/model/ 目录）。若有，将其作为参考实现并在设计中延续该模式。若项目无 DDD 实践，根据需求复杂度判断是否引入。

## 执行流程

当用户输入 `/ramer <需求描述>` 时：

### Step 1: 启动设计工作流

自动完成 **READ → ANALYZE → MODEL**：

- **READ**: 加载路径文档，检测目标区域架构风格（layered / hexagonal-ddd / hybrid），识别已有 aggregate、port、adapter、entity
- **ANALYZE**: 判定架构风格（CRUD/DDD/Hexagonal/Hybrid），定义 Bounded Context 和 Ubiquitous Language，列出所有新增/修改文件
- **MODEL**: 按判定风格产出设计契约：
  - DDD 风格 → aggregates、valueObjects、domainServices、ports、adapters、applicationServices、domainEvents
  - CRUD 风格 → entities（Entity Quartet 或等效模式）
  - 统一输出 implementationPlan（按 domain → infrastructure → application → interface 分层排序）

### Step 2: 呈现设计契约

清晰呈现：变更摘要、架构判定理由、DDD 分层图、文件清单、实现依赖顺序。

### Step 3: 人工确认

向用户确认设计契约：
- "批准，开始实现" (Recommended)
- "修改设计" → 收集意见，重新运行 Step 1
- "取消"

### Step 4: 实现（批准后）

自动完成 **EXECUTE → REVIEW**：

- **EXECUTE**: 按 design contract 逐文件实现，根据 layer 字段应用不同规则：
  - `domain` → 零框架依赖（禁止 Spring/MyBatis/Redis import）
  - `application` → @Service，注入 port 接口（非 adapter 类）
  - `infrastructure` → @Component，ACL 映射，实现 port 接口
  - `interface` → @RestController，权限注解
- **REVIEW**: 运行 fitness gate（如有）+ DDD 专项检查：
  - domain/model/ 和 domain/service/ 零框架 import
  - Application service 注入的是 port 接口还是 adapter 类
  - Adapter 是否有 ACL 映射说明
  - Port 是否有存在理由的 Javadoc
  - Mapper/XML/Entity 三方一致性（如适用）

### Step 5: 呈现结果

创建/修改的文件 + 质量检查通过/失败 + 修复建议。

## DDD 依赖规则（不可协商）

```
domain/     → 依赖 NOTHING（纯 Java，无 Spring/MyBatis/Redis import）
port/       → 依赖 domain（方法签名只用 domain 类型）
adapter/    → 依赖 port + 基础设施（@Component，Spring DI）
application → 依赖 domain + port（注入 port 接口，绝不注入 adapter）
controller  → 依赖 application（调用 application service）
```

## 编码原则

- **抽象优先**：契约先于实现，接口先于具体类
- **组合优于继承**：继承深度 ≤ 1，优先 DI + 组合
- **多态优于分支**：嵌套 `if/else` ≥ 2 层或 `switch` ≥ 3 分支 → 考虑 Strategy/Factory/Handler 模式
- **不可变优先**：Value Object 全 final 字段，无 setter
- **ACL 隔离**：Adapter 必须做反腐蚀映射，不泄露基础设施类型

## 参考实现

若项目的 CLAUDE.md 中声明了 DDD 参考实现模块，请以该模块为范本。若未声明，以项目中最接近 DDD 模式的模块为参考。
